export const DEFAULT_USER_MENU = [
  'generation',
  'video-pipeline',
];

export const DEFAULT_USER_API = [
  'generation',
  'video-pipeline',
  'browser-automation',
];

export const DEFAULT_USER_QUEUE = [];

export const DEFAULT_USER_JOB = [];

export const DEFAULT_ALLOWED_AI_PROVIDERS = [];

export const DEFAULT_ALLOWED_BROWSER_AUTOMATIONS = [];

export const DEFAULT_USER_SETTINGS = {
  generation: {},
  videoPipeline: {},
  scheduler: {},
};
